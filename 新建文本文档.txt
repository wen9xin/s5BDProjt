("SELECT A.Pays, G.gold,S.silver,B.bronze"
                                    "FROM AllPays A LEFT OUTER JOIN GoldEtPay G ON(A.pays=G.pays)"
                                    "LEFT OUTER JOIN SilverEtPay S ON (A.pays=S.pays)"
                                    "LEFT OUTER JOIN BronzeEtPay B ON (A.pays=B.pays)"
                                    "GROUP BY A.pays")

("SELECT A.Pays as num, G.gold as nbOr ,S.silver as nbArgent,B.bronze as nbBronze"
                                    "FROM AllPays A LEFT OUTER JOIN GoldEtPay G ON(A.pays=G.pays)"
                                    "LEFT OUTER JOIN SilverEtPay S ON (A.pays=S.pays)"
                                    "LEFT OUTER JOIN BronzeEtPay B ON (A.pays=B.pays)"
                                    "GROUP BY A.pays")

("SELECT pays, nbOr ,nbArgent,nbBronze"
                                    "FROM ResPays")


("SELECT COUNT(R.gold) as gold, I.pays as pays FROM LesResultats R LEFT OUTER JOIN InsEtPay I ON (R.gold=I.pays) GROUP BY pays;")

("SELECT A.Pays as pays, G.gold as nbOr ,S.silver as nbArgent,B.bronze as nbBronze"
                                    "FROM AllPays A LEFT OUTER JOIN GoldEtPay G ON(A.pays=G.pays)"
                                    "LEFT OUTER JOIN SilverEtPay S ON (G.pays=S.pays)"
                                    "LEFT OUTER JOIN BronzeEtPay B ON (S.pays=B.pays)"
                                    "GROUP BY A.pays")